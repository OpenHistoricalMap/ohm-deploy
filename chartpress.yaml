charts:
  - name: ohm
    imagePrefix: openhistoricalmap/

    images:
      web:
        valuesPath: osmSeed.web.image
      db:
        valuesPath: osmSeed.db.image
      tiler-db:
        valuesPath: osmSeed.tilerDb.image
      tiler-server:
        valuesPath: osmSeed.tilerServer.image
      
