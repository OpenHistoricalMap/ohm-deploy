charts:
  - name: ohm
    imagePrefix: ghcr.io/openhistoricalmap/
    images:
      web:
        valuesPath: osm-seed.web.image
      db:
        valuesPath: osm-seed.db.image
      tiler-db:
        valuesPath: osm-seed.tilerDb.image
      tiler-imposm:
        valuesPath: osm-seed.tilerImposm.image
      tiler-server:
        valuesPath: osm-seed.tilerServer.image
      tasking-manager-api:
        valuesPath: osm-seed.tmApi.image
      tiler-cache:
        valuesPath: ohm.tilerCache.image
      cgimap:
        valuesPath: ohm.cgimap.image
      osmcha-frontend:
        valuesPath: osm-seed.osmchaWeb.image
