worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /run/nginx/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log off;
    sendfile on;
    keepalive_timeout 65;

    upstream martin {
        server 127.0.0.1:3000;
        keepalive 32;
    }

    server {
        listen 80;

        # Health and catalog
        location /catalog {
            proxy_set_header Host $http_host;
            proxy_pass http://martin;
        }
        location /health {
            proxy_set_header Host $http_host;
            proxy_pass http://martin;
        }

        # Tegola-compatible: /maps/osm/land_ohm_lines/{z}/{x}/{y}.pbf
        location ~ ^/maps/osm/land_ohm_lines(/\d+/\d+/\d+)\.pbf$ {
            proxy_set_header Host $http_host;
            proxy_pass http://martin/land_ohm_lines$1;
        }
        location ~ ^/maps/osm/land_ohm_lines(/.*)?$ {
            proxy_set_header Host $http_host;
            proxy_pass http://martin/land_ohm_lines$1;
        }

        # Direct: /land_ohm_lines/{z}/{x}/{y}
        location ~ ^/land_ohm_lines(/\d+/\d+/\d+)\.pbf$ {
            proxy_set_header Host $http_host;
            proxy_pass http://martin/land_ohm_lines$1;
        }
        location ~ ^/land_ohm_lines(/.*)?$ {
            proxy_set_header Host $http_host;
            proxy_pass http://martin/land_ohm_lines$1;
        }

        # Viewer
        location = / {
            default_type text/html;
            root /app/static;
            try_files /index.html =404;
        }

        # Fallback
        location / {
            proxy_set_header Host $http_host;
            proxy_pass http://martin;
        }
    }
}
