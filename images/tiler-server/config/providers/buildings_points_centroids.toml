[[providers.layers]]
name = "buildings_points_centroids_z14"
geometry_fieldname = "geometry"
geometry_type = "point"
id_fieldname = "osm_id"
sql = """
SELECT 
    ST_AsMVTGeom(geometry, !BBOX!) AS geometry, 
    osm_id, 
    name, 
    type, 
    height,
    area,
    source,
    NULLIF(start_date, '') AS start_date, 
    NULLIF(end_date, '') AS end_date, 
    isodatetodecimaldate(pad_date(start_date, 'start'), FALSE) AS start_decdate, 
    isodatetodecimaldate(pad_date(end_date, 'end'), FALSE) AS end_decdate,
    {{LENGUAGES}}
FROM 
    mview_buildings_points_centroids_z14
WHERE 
    geometry && !BBOX!
"""

[[providers.layers]]
name = "buildings_points_centroids_z15"
geometry_fieldname = "geometry"
geometry_type = "point"
id_fieldname = "osm_id"
sql = """
SELECT 
    ST_AsMVTGeom(geometry, !BBOX!) AS geometry, 
    osm_id, 
    name, 
    type, 
    height,
    area,
    source,
    NULLIF(start_date, '') AS start_date, 
    NULLIF(end_date, '') AS end_date, 
    isodatetodecimaldate(pad_date(start_date, 'start'), FALSE) AS start_decdate, 
    isodatetodecimaldate(pad_date(end_date, 'end'), FALSE) AS end_decdate,
    {{LENGUAGES}}
FROM 
    mview_buildings_points_centroids_z15
WHERE 
    geometry && !BBOX!
"""

[[providers.layers]]
name = "buildings_points_centroids_z16"
geometry_fieldname = "geometry"
geometry_type = "point"
id_fieldname = "osm_id"
sql = """
SELECT 
    ST_AsMVTGeom(geometry, !BBOX!) AS geometry, 
    osm_id, 
    name, 
    type, 
    height,
    area,
    source,
    NULLIF(start_date, '') AS start_date, 
    NULLIF(end_date, '') AS end_date, 
    isodatetodecimaldate(pad_date(start_date, 'start'), FALSE) AS start_decdate, 
    isodatetodecimaldate(pad_date(end_date, 'end'), FALSE) AS end_decdate,
    {{LENGUAGES}}
FROM 
    mview_buildings_points_centroids_z16
WHERE 
    geometry && !BBOX!
"""

[[providers.layers]]
name = "buildings_points_centroids_z17"
geometry_fieldname = "geometry"
geometry_type = "point"
id_fieldname = "osm_id"
sql = """
SELECT 
    ST_AsMVTGeom(geometry, !BBOX!) AS geometry, 
    osm_id, 
    name, 
    type, 
    height,
    area,
    source,
    NULLIF(start_date, '') AS start_date, 
    NULLIF(end_date, '') AS end_date, 
    isodatetodecimaldate(pad_date(start_date, 'start'), FALSE) AS start_decdate, 
    isodatetodecimaldate(pad_date(end_date, 'end'), FALSE) AS end_decdate,
    {{LENGUAGES}}
FROM 
    mview_buildings_points_centroids_z17
WHERE 
    geometry && !BBOX!
"""

[[providers.layers]]
name = "buildings_points_centroids_z18_20"
geometry_fieldname = "geometry"
geometry_type = "point"
id_fieldname = "osm_id"
sql = """
SELECT 
    ST_AsMVTGeom(geometry, !BBOX!) AS geometry, 
    osm_id, 
    name, 
    type, 
    height,
    area,
    source,
    NULLIF(start_date, '') AS start_date, 
    NULLIF(end_date, '') AS end_date, 
    isodatetodecimaldate(pad_date(start_date, 'start'), FALSE) AS start_decdate, 
    isodatetodecimaldate(pad_date(end_date, 'end'), FALSE) AS end_decdate,
    {{LENGUAGES}}
FROM 
    mview_buildings_points_centroids_z18_20
WHERE 
    geometry && !BBOX!
"""

#######Maps
[[maps.layers]]
name = "buildings_points_centroids"
provider_layer = "osm.buildings_points_centroids_z14"
min_zoom = 14
max_zoom = 14

[[maps.layers]]
name = "buildings_points_centroids"
provider_layer = "osm.buildings_points_centroids_z15"
min_zoom = 15
max_zoom = 15

[[maps.layers]]
name = "buildings_points_centroids"
provider_layer = "osm.buildings_points_centroids_z16"
min_zoom = 16
max_zoom = 16

[[maps.layers]]
name = "buildings_points_centroids"
provider_layer = "osm.buildings_points_centroids_z17"
min_zoom = 17
max_zoom = 17

[[maps.layers]]
name = "buildings_points_centroids"
provider_layer = "osm.buildings_points_centroids_z18_20"
min_zoom = 18
max_zoom = 20
